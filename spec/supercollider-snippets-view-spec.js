'use babel';

import SupercolliderSnippetsView from '../lib/supercollider-snippets-view';

describe('SupercolliderSnippetsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
